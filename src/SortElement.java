public class SortElement {
    //This class makes an object that holds a string value representing a counrty and an int value which is the result from the quiz..
    //This allows the countries to be sorted by our amazing merge sorter in order of the results from the quiz
    public String text;
    public int value;
    public SortElement(String text, int value) {
        this.text = text;
        this.value =  value;
    }
}
